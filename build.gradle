    - name: Build with Gradle
      run: |
        if [ -f "gradlew" ]; then
          chmod +x gradlew
          ./gradlew assembleDebug
        elif [ -d "app" ]; then
          gradle assembleDebug
        else
          echo "Mencari file build project..."
          ./gradlew assembleDebug || gradle assembleDebug
        fi
